UPDATE own_83650_muse.muse_diffusion_client_field_mappings
SET data_format = 'dd/MM/yyyy',
    updated_at = current_timestamp
WHERE data_format = 'dd/mm/yyyy'
AND data_type = 'DATETIME';SELECT field_mapping_id, field_name, data_type, data_format, diffused_data
FROM own_83650_muse.muse_diffusion_client_field_mappings
WHERE data_format = 'dd/mm/yyyy'
AND data_type = 'DATETIME';
